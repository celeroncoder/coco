# Populated by `bunx convex dev`.
CONVEX_DEPLOYMENT=
CONVEX_URL=

# Set as a Convex deployment env var (Convex dashboard → Settings → Environment Variables),
# NOT in this .env file. Value is your Clerk Frontend API URL, e.g.
#   https://verb-noun-00.clerk.accounts.dev   (dev)
#   https://clerk.<your-domain>.com           (prod)
# CLERK_JWT_ISSUER_DOMAIN=
